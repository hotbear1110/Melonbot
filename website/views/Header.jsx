const { Component } = require('react');
const React = require('react');

module.exports = class Header extends Component {
    render() {
    return(
        <>
        <header>
            <a href="/">Home</a>
        </header>
        </>
        )
    }
}